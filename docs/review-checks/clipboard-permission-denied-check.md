# Clipboard Permission Denied Check

- Deny clipboard permission when copying a metadata or explorer link.
- Confirm the app shows a manual-copy fallback.
- Verify the fallback text contains the same URL as the copy action.
