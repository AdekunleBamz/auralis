# Deploy Preview Metadata Origin Check

- Mint or simulate a token URI from a preview deployment.
- Confirm `/api/nft` links resolve on the same preview host.
- Record the checked origin so reviewers can reproduce the metadata response.
