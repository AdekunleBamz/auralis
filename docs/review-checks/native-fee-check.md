# Native Fee Check

Before testing the web wallet path, confirm `NEXT_PUBLIC_AURALIS_MINT_FEE_WEI` matches the value configured on `AuralisGenesis`.

The default README value is `2000000000000000`, which should display to reviewers as `0.002 CELO + gas`.
