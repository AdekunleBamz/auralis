# IPFS Timeout Check

Simulate a slow pinning response and confirm the loading copy remains visible.
Record the timeout duration, retry path, and final user-facing status.
Attach only non-sensitive request identifiers.
