# Stable Contract Check

Confirm `NEXT_PUBLIC_AURALIS_STABLE_NFT_ADDRESS` points to the deployed `AuralisGenesisStable` contract before testing MiniPay minting.

If the value changed after redeploying, update the root README and deployment guide in the same release note set.
