# Mint Fee Env Boundary Check

- Set the CELO and USDm mint fee values to the lowest supported non-zero test values.
- Confirm the UI rounds and labels both fees correctly.
- Verify a missing fee env value keeps the mint path blocked instead of using a silent default.
