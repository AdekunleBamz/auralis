# Production URL Check

Confirm `NEXT_PUBLIC_APP_URL` matches the deployed production domain before publishing agent metadata or NFT metadata links.

If the preview uses a temporary URL, keep that value out of production release notes unless the preview itself is being reviewed.
