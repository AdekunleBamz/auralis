# API NFT Long Prompt Check

- Request metadata with a prompt near the UI length limit.
- Confirm the response remains valid JSON and includes the expected prompt-derived fields.
- Verify long query strings are not truncated by the deployed host.
