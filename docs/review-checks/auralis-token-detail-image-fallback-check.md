# Auralis Token Detail Image Fallback Check

Open a token detail view whose image gateway is unavailable.
Confirm the fallback keeps token id and owner information visible.
Verify retrying the gateway does not duplicate the detail route history.
