# Agent Wallet Sync Check

- Confirm the agent wallet in `public/.well-known/agent.json` matches the production operator wallet.
- Verify the same address is referenced in deployment notes before registering the agent.
- Record the checksum-cased address used for the release.
