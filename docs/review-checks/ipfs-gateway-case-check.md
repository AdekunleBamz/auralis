# IPFS Gateway Case Check

- Open generated metadata whose image URL contains mixed-case path segments.
- Confirm gateway normalization does not change the image reference.
- Record any gateway that fails so fallback ordering can be reviewed.
