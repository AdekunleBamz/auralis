# Metadata JSON Schema Check

Validate that generated NFT metadata keeps `name`, `description`, `image`, and `attributes` present.
Compare the browser preview with the uploaded JSON payload before release.
Record the metadata URL used for the schema check.
