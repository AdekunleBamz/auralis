# Logo Asset Check

Open `public/auralis-logo.svg` from the production domain and confirm it loads before publishing agent metadata that references it.

The same hosted logo should be used by the public agent manifest and any release notes that preview Auralis branding.
