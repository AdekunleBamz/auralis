# Compose Empty Prompt Check

Submit an empty compose prompt and confirm the API returns a helpful validation response.
The response should not include stack traces, provider payloads, or private environment values.
