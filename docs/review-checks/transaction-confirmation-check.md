# Transaction Confirmation Check

After minting, wait for the wallet to return a transaction hash and confirm the app shows the success state only for that hash.

Capture the hash in the release note so the preview can be matched to the on-chain record later.
