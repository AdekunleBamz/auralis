# Network Add Chain Fallback Check

- Test a browser wallet that has not added the selected Celo chain.
- Confirm the add-chain request uses the documented RPC and explorer values.
- Verify a denied add-chain request leaves the app in a retryable state.
