# Metadata Image Protocol Check

- Inspect one accepted metadata response before minting.
- Confirm the `image` field uses a protocol supported by the target marketplace.
- Record the exact image URL with the mint transaction evidence.
