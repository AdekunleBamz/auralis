# IPFS Metadata Image Consistency Check

- Open a generated metadata JSON document and copy the `image` URI.
- Confirm the image loads through the configured gateway and matches the preview shown before minting.
- Verify retrying the metadata endpoint does not swap the image unexpectedly.
