# Mint Fee Display Check

Compare the displayed mint fee with the configured native and stable fee values.
Record the token symbol, formatted amount, and source environment variable.
Flag any mismatch before production promotion.
