# Remix Path Check

Before using the deployment guide, confirm the referenced SDK contract paths still exist in the sibling SDK checkout or published SDK package.

If the paths moved, update the app deployment docs before asking another reviewer to redeploy contracts.
