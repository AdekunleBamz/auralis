# Preview Build Check

Before sharing a Vercel preview, run `npm run lint` and `npm run build` from a clean checkout that uses the same environment variable set as the preview.

Attach the command results to the release note when contract or fee configuration changed.
