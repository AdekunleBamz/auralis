# Chain ID Doc Check

When switching between Celo Mainnet and a test deployment, confirm the chain ID in `.env.local` is also reflected in the deployment notes being reviewed.

Use `42220` for Celo Mainnet release checks and only use the testnet value when the preview is explicitly a testnet preview.
