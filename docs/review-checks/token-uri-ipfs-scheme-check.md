# Token URI IPFS Scheme Check

- Inspect token URI values after minting.
- Confirm IPFS-backed metadata uses the expected scheme or gateway format.
- Verify marketplace links resolve the same metadata JSON.
