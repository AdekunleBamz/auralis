# Environment Sync Check

Before a production preview is shared, compare the contract values in `.env.local` with the addresses and fees listed in the root README and deployment guide.

Confirm the native mint address, stable mint address, chain ID, stable fee token, and stable fee amount all describe the same deployment set.
