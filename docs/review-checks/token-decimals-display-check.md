# Token Decimals Display Check

Verify stable fee amounts render with the token decimals configured for the selected fee token.
Record the raw base-unit value and formatted display value.
Compare USDm values separately from other stablecoins.
