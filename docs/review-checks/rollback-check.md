# Rollback Check

Before a production deploy, keep the previous working contract addresses and preview URL available in the release handoff.

If a mint issue appears after launch, reviewers should be able to restore the last known good environment variables quickly.
