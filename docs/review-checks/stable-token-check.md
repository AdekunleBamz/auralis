# Stable Token Check

Before a MiniPay release test, confirm the configured stable fee token is still Celo Mainnet USDm.

The address should be `0x765DE816845861e75A25fCA122bb6898B8B1282a` in `.env.local`, `.env.example`, `src/lib/celo.ts`, and the deployment notes.
