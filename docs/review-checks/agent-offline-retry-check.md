# Agent Offline Retry Check

- Simulate the compose endpoint failing while the wallet stays connected.
- Confirm retry copy tells the creator the draft was not minted or saved.
- Verify a successful retry replaces the stale error before minting is enabled.
