# Browser Network Check

In a normal browser wallet, start from the wrong network and confirm the app requests the configured Celo network before minting.

Do not run this check in MiniPay, because MiniPay should stay on its configured Celo session without a chain switch prompt.
