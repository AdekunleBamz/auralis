# Prompt Normalization Check

Before approving a mint preview, compose a prompt with extra spacing and confirm the displayed prompt matches the normalized draft returned by the agent.

The mint button should only be used after the visible prompt and draft prompt still describe the same artifact.
