# Support Handoff Check

Before publishing a production link, record where users should report failed compose, approval, or mint attempts.

Include the preview URL, wallet mode, transaction hash when available, and the prompt that was being minted.
