# Auralis Mint Retry After Revert Check

Use a test contract response that reverts the mint transaction.
Confirm the UI reports the failed transaction and keeps a clear retry path.
Verify a later successful retry refreshes the gallery only once.
