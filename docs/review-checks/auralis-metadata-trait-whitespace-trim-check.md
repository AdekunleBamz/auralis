# Auralis Metadata Trait Whitespace Trim Check

Enter trait names and values with leading and trailing spaces.
Confirm the saved metadata trims accidental whitespace.
Verify intentional internal spacing is preserved in the preview.
