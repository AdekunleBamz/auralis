# IPFS Gateway Check

- Open the minted metadata and media through the configured IPFS gateway.
- Confirm each URI resolves without requiring a local gateway override.
- Verify gateway failures show safe retry or fallback copy.
- Save the gateway URL and response status in QA notes.
