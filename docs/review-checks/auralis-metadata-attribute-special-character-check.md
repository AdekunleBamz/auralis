# Auralis Metadata Attribute Special Character Check

Generate metadata with punctuation and accented words in trait values.
Confirm the preview and JSON payload encode the values consistently.
Verify the metadata remains valid JSON after upload.
