# Contract Missing Check

Temporarily review a preview with a missing contract address and confirm the release notes explain which environment variable is required.

Do this only in a non-production environment so reviewers can verify the handoff without blocking the live app.
