# Auralis Mint Preflight Metadata Check

Attempt to mint before metadata has a valid URL.
Confirm the app blocks the transaction and keeps the generated artwork preview visible.
Verify the user can retry metadata upload without reconnecting the wallet.
