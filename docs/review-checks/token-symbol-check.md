# Token Symbol Check

After contract deployment, confirm the token symbol shown by Remix matches the symbol referenced in reviewer notes.

For the current Auralis contracts, reviewers should expect `AURA` unless a release intentionally changes the collection symbol.
