# Loading State Check

During preview review, start an agent compose request and a mint request on a slow connection.

Confirm the loading state remains visible until the request finishes and that a reviewer cannot accidentally submit the same action twice.
