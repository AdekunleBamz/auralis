# App URL Metadata Check

Build metadata links with `NEXT_PUBLIC_APP_URL` set and unset.
Record the resulting origin and confirm preview links stay on the production domain.
Flag any localhost URL before release.
