# Fee Symbol Check

Confirm `NEXT_PUBLIC_AURALIS_STABLE_FEE_SYMBOL` matches the stable fee token shown to MiniPay users.

For the default Celo Mainnet setup, the UI should label the stable fee as `USDm` and not as CELO or cUSD.
