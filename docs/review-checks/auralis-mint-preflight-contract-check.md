# Auralis Mint Preflight Contract Check

Run the mint flow with the collection contract address unset.
Confirm the preflight state blocks minting before any wallet prompt opens.
Verify the error points to the missing contract configuration.
