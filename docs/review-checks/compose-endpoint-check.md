# Compose Endpoint Check

Before sharing a preview, submit a short prompt to `/api/agent/compose` and confirm the response includes `ok: true`, a draft, a prompt hash, and the Celo ERC-8004 metadata block.

This check catches endpoint configuration issues before reviewers reach the mint step.
