# Metadata Animation URL Empty Check

- Mint static artwork with no animation URL.
- Confirm metadata omits optional animation fields when they are empty.
- Verify marketplaces still render the image field as the primary asset.
