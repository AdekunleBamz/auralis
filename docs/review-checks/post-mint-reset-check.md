# Post-Mint Reset Check

After a successful mint, change the prompt and confirm the app requires a fresh compose step before another mint.

This prevents reviewers from accidentally minting stale metadata after editing the visible prompt.
