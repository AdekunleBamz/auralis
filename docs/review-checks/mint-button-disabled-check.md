# Mint Button Disabled Check

Verify the mint action stays disabled while metadata is incomplete or uploading.
Record which field made the form invalid and the visible helper copy.
Confirm the button enables after the missing value is restored.
