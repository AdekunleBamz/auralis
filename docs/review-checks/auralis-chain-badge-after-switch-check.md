# Auralis Chain Badge After Switch Check

Connect on an unsupported network and then switch back to Celo.
Confirm the chain badge updates without requiring a full page refresh.
Verify mint controls only unlock after the supported chain is active.
