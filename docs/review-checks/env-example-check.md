# Env Example Check

When a required public environment variable changes, update `.env.example` before sharing setup instructions with another reviewer.

Compare the example file with the README deployment block so new reviewers do not copy stale contract or fee keys.
