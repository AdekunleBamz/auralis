# Stable Fee Check

Confirm the configured stablecoin mint fee matches the amount set on `AuralisGenesisStable`.

For the default USDm setup, `NEXT_PUBLIC_AURALIS_STABLE_FEE_AMOUNT` should stay aligned with the contract value `200000000000000` before a MiniPay release test.
