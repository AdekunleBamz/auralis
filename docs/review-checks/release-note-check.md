# Release Note Check

Before handing off a preview, record the app commit, deployed URL, native contract address, stable contract address, and wallet used for the smoke test.

Keep the release note with the preview so later reviews can trace which deployment was tested.
