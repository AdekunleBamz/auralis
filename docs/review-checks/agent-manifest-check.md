# Agent Manifest Check

Before registering or reviewing the ERC-8004 agent metadata, open `public/.well-known/agent.json` and confirm the hosted image, compose endpoint, wallet address, and `chainId` all match the intended production deployment.

The placeholder zero wallet address should be replaced before the agent is registered publicly.
