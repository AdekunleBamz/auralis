# Metadata Retry Check

Retry a failed metadata upload and confirm the second attempt replaces the failed status.
Record the first error, retry trigger, and final metadata URL.
Do not reuse a failed CID in release notes.
