# Auralis Compose Invalid JSON Check

Mock the compose service returning malformed JSON.
Confirm Auralis shows a service error without losing the prompt.
Verify no metadata upload is attempted from the malformed response.
