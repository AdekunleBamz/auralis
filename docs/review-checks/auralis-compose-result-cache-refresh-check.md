# Auralis Compose Result Cache Refresh Check

Generate artwork and refresh the browser before minting.
Confirm the cached result reloads with the prompt and metadata draft.
Verify starting a new composition replaces only the intended draft fields.
