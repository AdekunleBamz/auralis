# SDK Version Check

Before a release review, confirm the installed `@bamzzstudio/auralis-sdk` version contains the contract ABI and helper behavior expected by the app.

If the sibling SDK is used locally, note that in the release handoff so reviewers do not confuse it with the published npm package.
