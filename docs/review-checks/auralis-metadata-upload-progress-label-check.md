# Auralis Metadata Upload Progress Label Check

Upload metadata with throttled network conditions.
Confirm the progress label distinguishes uploading metadata from minting onchain.
Verify a failed upload returns the user to the metadata step, not the wallet step.
