# Duplicate Submit Check

While a compose or mint request is pending, click the same action again and confirm the app does not start a second duplicate request.

Use a test wallet and capture the screen state if duplicate transaction prompts appear.
