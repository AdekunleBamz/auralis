# Environment Missing Agent URL Check

- Build a preview without the agent compose URL configured.
- Confirm the app disables generation paths that require the missing endpoint.
- Verify minting existing approved artwork still has clear copy.
