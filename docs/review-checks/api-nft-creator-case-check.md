# API NFT Creator Case Check

- Request `/api/nft` with the same creator address in mixed and lowercase forms.
- Confirm accepted addresses produce stable metadata for the same prompt.
- Record any checksum-related rejection separately from hash mismatch behavior.
