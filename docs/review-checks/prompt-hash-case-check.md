# Prompt Hash Case Check

- Request metadata with the expected hash in uppercase and lowercase forms.
- Confirm valid hash casing is accepted consistently.
- Verify an actually different hash still returns a mismatch error.
